# Puerto del backend
PORT=3000

# Conexión PostgreSQL del backend (reemplazar con credenciales locales)
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/listafacil?schema=public"
JWT_SECRET="replace-with-a-secure-secret"
